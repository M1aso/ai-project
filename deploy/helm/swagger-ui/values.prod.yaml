ingress:
  host: "docs.yourdomain.com"  # Update with actual production domain
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
  tls:
    - secretName: docs-tls
      hosts:
        - docs.yourdomain.com