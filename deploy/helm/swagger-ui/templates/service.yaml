apiVersion: v1
kind: Service
metadata:
  name: {{ include "swagger-ui.fullname" . }}
  labels:
    app: {{ include "swagger-ui.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "swagger-ui.fullname" . }}