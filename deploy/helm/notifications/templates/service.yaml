apiVersion: v1
kind: Service
metadata:
  name: {{ include "svc.fullname" . }}
  labels: { app: {{ include "svc.fullname" . }} }
spec:
  type: ClusterIP
  selector: { app: {{ include "svc.fullname" . }} }
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
