image:
  repository: "ghcr.io/m1aso/notifications"
  tag: "dev"
  pullPolicy: IfNotPresent

service:
  port: 8000

imagePullSecrets:
  - name: ghcr-creds

ingress:
  enabled: false  # exposed only via api-gateway
